__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Agent worktrees. One per brief; see CLAUDE.md → Agents work in worktrees.
.claude/worktrees/
.claude/settings.local.json

# Fit artifacts and reports produced by running the tool.
*.irt
/scratch/

# Generated by scripts/: the run enumeration, the fetched responses, and the
# subsampled matrix. All rebuildable in ~35 seconds and large; scripts/README.md
# records the provenance of the numbers docs/validation.md quotes.
/helm_runs.json
/helm_responses.jsonl
/helm_12.jsonl
