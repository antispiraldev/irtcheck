"""The 2PL and its posterior — owned by wave1/fit.

This is the only package permitted to import torch or pyro. Everything else
reads a cached artifact instead; see CLAUDE.md → The lazy torch boundary.
"""
